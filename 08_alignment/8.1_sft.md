## 8.1 监督微调：教模型“怎么回答”

**监督微调**（Supervised Fine-Tuning，SFT）是将基础模型转化为能够按指令回答问题的助手模型的第一步。此刻的基础模型只会一件事——无差别地顺着给定文本往下续写；把这条本能改造成“被问就答”，就是本节的全部内容。至于答得好不好、安不安全，是 8.2 与 8.3 要处理的问题。

### 8.1.1 从续写到对话

预训练模型被训练来续写文本——给它“法国的首都是”，它会接着写“巴黎，位于塞纳河畔...”。但如果直接问它“法国的首都是什么？”，它可能续写出“这是一道地理题...”而非直接回答“巴黎”。

SFT 通过在**指令-回答对**上训练模型，教会它理解用户的意图并给出恰当格式的回答。训练数据的形式通常为：

```text
用户：法国的首都是什么？
助手：法国的首都是巴黎。
```

### 8.1.2 训练过程

SFT 在技术上与预训练完全相同——仍然是最小化下一个词元的预测损失。关键区别在于**训练数据的质量和格式**：

- 数据由高质量的指令-回答对组成（通常由人工标注或高质量模型生成）
- 损失只在**回答部分**计算，指令部分不参与损失（避免模型学习“如何提问”而非“如何回答”）
- 使用远小于预训练的学习率（如 1e-5 vs 预训练的 1e-4），避免破坏预训练学到的知识

一个常见的疑问是：既然底层都是反向传播和梯度下降，SFT 和预训练到底有什么本质区别？答案是**没有算法层面的区别——区别完全在于数据和超参数的选择所产生的效果**。预训练在万亿级通用文本上训练，模型学到的是语言的统计规律和世界知识，但不知道“被问问题时应该直接回答”；SFT 在精心构造的指令-回答对上训练，用极小的学习率微调权重，让模型在保留已有知识的同时，学会一种新的**输入-输出映射模式**——“收到指令 → 生成回答”。本质上，SFT 并没有“教”模型新知识，而是激活并重组预训练阶段已经学到的能力，使其以人类期望的格式展现出来。

#### 为什么学习率必须小一到两个数量级

上面那条“用远小于预训练的学习率”常被当成一条要背下来的经验值，但它其实是从一个量级不对称里直接推出来的：预训练看的是万亿级词元，SFT 看的是几万条样本——**两者相差四到六个数量级**。

回到 [6.1](../06_training_techniques/6.1_loss_optimizer.md) 的更新公式：一步更新的幅度由学习率乘以梯度决定，而权重最终落在哪里，取决于**沿途所有更新的累积**。预训练阶段，任何一条样本的贡献都被海量其他样本稀释，单条样本推不动权重；SFT 阶段样本数少了几个数量级，如果还用同样的学习率，少数几条样本就足以主导权重的移动方向——模型会飞快地学会这批数据的表面模式，同时把预训练里学到的、这批数据没覆盖到的能力一起挤掉。

这就是**灾难性遗忘**（catastrophic forgetting）：模型学会了新任务，却把旧能力弄丢了。小学习率不是为了“训得慢一点”，而是把每一步的位移压到足够小，让新的输入-输出映射长在旧能力之上，而不是覆盖它。同样的道理也解释了为什么 SFT 通常只跑一到几个 epoch：轮数越多，同一批样本被反复走过的累积位移越大，越接近覆盖而非叠加。这条约束的完整形态——它怎么表现、怎么被发现、怎么缓解——见 [8.5](8.5_practice.md)。

### 8.1.3 数据质量的影响

研究表明，SFT 的效果对数据**质量远比数量**更敏感。[LIMA 论文](https://arxiv.org/abs/2305.11206)用 1000 条精心编写的示例微调一个 **65B** 的 LLaMa 模型，就得到了在人类偏好评测上可与当时最强商用模型一较高下的结果，作者由此提出：模型几乎全部的知识都在预训练阶段学到，只需要有限的指令数据就能教会它把这些知识以高质量的形式输出。这被称为 **“少而精”原则**。

**但“1000 条”不是配方，读它必须连着读它的前提。** LIMA 那 1000 条是喂给一个 65B 模型的——“知识都在预训练里”这个结论，只有在预训练本身足够充分时才成立；同一套做法搬到一个小得多的基座上，缺的知识不会因为示例精良而补上。而且它的评判方式是人类偏好对比，不是知识密集型基准上的准确率。

把另一个锚点放在旁边，这层限制会更清楚：[Llama 2 论文](https://arxiv.org/abs/2307.09288)明确说自己的发现“与 Zhou 等人（即 LIMA）在精神上一致”，然而它给出的量是 **27,540 条**——“我们发现数万量级的 SFT 标注就足以取得高质量结果，收集到 27,540 条之后我们停止了标注”。两个锚点相差 27 倍，结论却是同一句“质量优先”。这恰恰说明：**能从这类研究里带走的是“到某个量级之后，继续堆数量的边际收益迅速衰减”这条关系，而不是某个可以照抄的条数。** 那个拐点在哪，取决于基座规模、任务分布与标注质量——Llama 2 团队还顺带记了一笔：不同标注平台与供应商产出的数据，会让下游模型表现出明显差异。

关键是每条训练样本都应该展示出理想的回答方式：格式恰当、内容准确、语气合适、涵盖边界情况的处理。

SFT 后的模型已经能够进行有效对话，但它的回答质量不够稳定——有时会生成有害、不准确或令人不满的内容。这推动了更进一步的对齐技术。
